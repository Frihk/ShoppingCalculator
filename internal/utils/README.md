# Utils

Keep this folder small. Only place truly shared helpers here when they do not belong to a domain or service.

Functions that may belong here if they are generic enough:

- `NowUTC`
  Return the current time in one standard format.
- `GenerateID`
  Create identifiers if they are not part of business rules.
- `ParseDate`
  Parse generic date strings.


